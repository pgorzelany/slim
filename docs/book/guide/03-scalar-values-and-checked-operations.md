# Scalar values and checked operations

SLIM's scalar core is intentionally small: `Bool`, `I64`, `U8`, and `Void`,
with canonical operators for arithmetic, comparison, and Boolean logic and
explicit built-ins for conversion and I/O.

## Scalar types

`Bool` contains `true` and `false`. `I64` is a signed 64-bit integer. `U8` is an
unsigned byte. `Void` is the result type for operations whose purpose is an
effect and cannot be stored. There are no implicit numeric conversions.

The scalar conformance fixture exercises every accepted scalar type and scalar
built-in:

<!-- slim-fixture: scalars -->

## Checked arithmetic

Arithmetic uses the canonical operators:

```slim
let sum: I64 = left + right
```

`+`, `-`, and `*` trap on overflow. Division and remainder
trap on a zero divisor; the minimum-integer divided by `-1` also traps.

<!-- slim-fixture: overflow -->

A runtime trap exits with status `70` and a stable message. It is observable
behavior: reduction and optimization may not erase or reorder it.

## Comparisons and Boolean operations

Integer comparison operators return `Bool`. `!`, `&&`, and `||` are canonical
Boolean operators. Operands retain left-to-right evaluation.

## Explicit conversion

`u8.to_i64` widens a byte. `i64.to_u8` checks that the value is within the byte
range and traps otherwise.

<!-- slim-fixture: u8-conversion-bounds -->

## Compiler guarantees

The checker rejects argument and result type mismatches. The runtime cannot
silently wrap checked arithmetic. The analyzer may remove a trap hazard only
when it has a positive fact for that exact checked node; missing or exhausted
evidence remains unknown.

## Deliberate omissions

There are no implicit conversions, named operator aliases, unchecked arithmetic,
floating-point values, or user-selectable overflow modes in the accepted
surface. The exact built-in list is generated by `./slimc builtins` and recorded
in [Core](../../CORE.md).
