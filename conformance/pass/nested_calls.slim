module nested-calls

fn length(values: Vec[I64]) -> I64:
  vec.len(values)

fn main(args: Vec[Bytes]) -> I64 effects[alloc io]:
  let shown: Unit = io.print-i64(length(vec.new()))
  let newline: Unit = io.println("")
  0
