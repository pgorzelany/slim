module monotonic_clock

fn main(args: Vec[Bytes]) -> I64 effects[io]:
  let first: I64 = io.monotonic_ms()
  let second: I64 = io.monotonic_ms()
  if second >= first:
    io.println("OK")
    0
  else:
    1
