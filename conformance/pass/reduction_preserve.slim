module reduction-preserve

fn main(args: Vec[Bytes]) -> I64 effects[alloc io partial]:
  let values: Vec[I64] = vec.new()
  let pushed: Unit = vec.push(values 1)
  let moved: Vec[I64] = values
  let visible: Unit = io.println("kept")
  i64.add(i64.add(9223372036854775807 1) 0)
