module duplicate_variant_arm

enum Maybe:
  None
  Some(I64)

fn unwrap(value: Maybe) -> I64:
  match value:
    None:
      0
    None:
      1

fn main(args: Vec[Bytes]) -> I64:
  unwrap(Maybe::None())
