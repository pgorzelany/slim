module move-from-inout

fn take(values: Vec[I64]) -> I64:
  vec.len(values)

fn inspect(inout values: Vec[I64]) -> I64:
  take(values)

fn main(args: Vec[Bytes]) -> I64 effects[alloc]:
  let values: Vec[I64] = vec.new()
  inspect(values)
