module frozen_source_use

fn length(values: Vec[U8]) -> I64:
  vec.len(values)

fn main(args: Vec[Bytes]) -> I64 effects[alloc]:
  let values: Vec[U8] = vec.new()
  let frozen: Bytes = bytes.freeze(values)
  length(values)
