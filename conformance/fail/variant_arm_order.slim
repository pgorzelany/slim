module variant_arm_order

enum Maybe:
  None
  Some(I64)

fn unwrap(value: Maybe) -> I64:
  match value:
    Some(number):
      number
    None:
      0

fn main(args: Vec[Bytes]) -> I64:
  unwrap(Maybe::None())
