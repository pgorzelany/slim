module shared_variant_payload_mutation

enum OwnerChoice:
  none
  some(Vec[I64])

fn invalid(value: OwnerChoice) -> Void effects[alloc]:
  match value:
    none:
      void
    some(values):
      vec.push(@values, 42)

fn main(args: Vec[Bytes]) -> I64:
  0
