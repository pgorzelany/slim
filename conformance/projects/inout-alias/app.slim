module app

fn pair(inout left: Vec[I64] inout right: Vec[I64]) -> Unit:
  unit

fn main(args: Vec[Bytes]) -> I64 effects[alloc]:
  let values: Vec[I64] = vec.new()
  let used: Unit = pair(values values)
  0
